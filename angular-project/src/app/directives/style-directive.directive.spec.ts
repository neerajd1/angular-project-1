import { StyleDirectiveDirective } from './style-directive.directive';

describe('StyleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
